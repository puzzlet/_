[include]
    # works both in Linux/Windows
    path = .gitconfig_private
[core]
    excludesfile = ~/.gitignore
    autocrlf = false
    eol = lf
[push]
    default = upstream
[diff]
    tool = vimdiff
[merge]
    tool = vimdiff
[color]
    ui = auto
[bash]
    showDirtyState = true
